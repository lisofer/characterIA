name: Android APK

on:
  push:
    branches: [ main, "feature/**" ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-24.04
    timeout-minutes: 25

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - name: Install required Android SDK packages
        shell: bash
        run: |
          SDKMANAGER="/usr/local/lib/android/sdk/cmdline-tools/latest/bin/sdkmanager"
          yes | "$SDKMANAGER" --licenses >/dev/null || true
          "$SDKMANAGER" "platform-tools" "platforms;android-36" "build-tools;36.0.0"

      - name: Set up Gradle 8.13
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.13"

      - name: Build debug APK
        run: gradle --no-daemon --stacktrace :app:assembleDebug

      - name: Publish versioned debug APK
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/heads/feature/')
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          TAG="android-debug-${GITHUB_RUN_NUMBER}"
          SOURCE_APK="app/build/outputs/apk/debug/app-debug.apk"
          if [[ "$GITHUB_REF_NAME" == "feature/characteria-vision-smooth" ]]; then
            APK="CharacterIA-Vision-Smooth-debug-${GITHUB_RUN_NUMBER}.apk"
            TITLE="CharacterIA Visión Smooth Debug ${GITHUB_RUN_NUMBER}"
            NOTES="CharacterIA Visión Smooth 0.5: usa un único MP4. Analiza automáticamente el mismo video para elegir un tramo corto con la boca relajada como idle, lo reproduce en loop y usa el resto del archivo como biblioteca de bocas. Se elimina por completo el parche grande de mandíbula de 0.4. El tramo idle se reanaliza densamente (~30 Hz) y queda cacheado para seguir la boca con mayor precisión. Mantiene lipsync PCM a ~60 Hz y no usa MuseTalk ni modelos neuronales pesados en runtime."
          elif [[ "$GITHUB_REF_NAME" == "feature/characteria-vision" ]]; then
            APK="CharacterIA-Vision-debug-${GITHUB_RUN_NUMBER}.apk"
            TITLE="CharacterIA Visión Debug ${GITHUB_RUN_NUMBER}"
            NOTES="APK experimental de CharacterIA Visión. Se instala junto a CharacterIA estable."
          else
            APK="CharacterIA-debug-${GITHUB_RUN_NUMBER}.apk"
            TITLE="CharacterIA Android Debug ${GITHUB_RUN_NUMBER}"
            NOTES="APK de prueba generado automáticamente desde $GITHUB_REF_NAME."
          fi
          cp "$SOURCE_APK" "$APK"
          gh release create "$TAG" "$APK" \
            --repo "$GITHUB_REPOSITORY" \
            --target "$GITHUB_SHA" \
            --title "$TITLE" \
            --notes "$NOTES" \
            --prerelease
